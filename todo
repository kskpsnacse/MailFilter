add_user - a command takes care of adding users to the table with auth details, optional delay start

delete from actions where 1 = 1;
delete from action_scheduler where 1 = 1;
delete from completed_actions where 1 = 1;
delete from filtered_mails where 1 = 1;
delete from filter_scheduler where 1 = 1;


apt-get update
DEBIAN_FRONTEND=noninteractive apt-get install -y tzdata
apt-get install -y vim curl
apt-get install -y python3 python3-pip git
apt-get install -y python3.12-venv
cd /home/ubuntu
git clone https://github.com/kskpsnacse/MailFilter.git
cd MailFilter
python3 -m venv ./venv
source venv/bin/activate
pip install mypy sqlalchemy dataclasses-json
pip install google-api-python-client google-auth-httplib2 google-auth-oauthlib
mypy main.py


download client credentials.json by following the steps provided here - https://developers.google.com/gmail/api/quickstart/python

mkdir user_creds
# paste the contents of credentials.json by using next command
vi user_creds/credentials.json
